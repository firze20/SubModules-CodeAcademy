const promise = new Promise((resolve, reject) => {
    //resolve e reject são ambos funções

    //resolve muda o valor da Promise (Pending) para FullFill
    //reject muda o valor da Promise (Pending) para Rejected
});